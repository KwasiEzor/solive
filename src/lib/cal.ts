/**
 * Cal.com booking link (SLV — Phase 5 booking). Not a secret — this is the
 * public booking page URL, safe to inline. CAL_API_KEY (env, server-only)
 * is registered separately for future use (e.g. a webhook syncing
 * completed bookings into `leads`), not needed for this plain iframe embed.
 */
export const CAL_BOOKING_URL = "https://cal.com/kwasi-ezor-khw4he/30min";
